# Trivy 無視リスト。
# ここに載せるのは「自前リポジトリでは修正不能」な脆弱性のみ（＝distroless ベースの OS パッケージ）。
# ライブラリ（node-pkg 等）の脆弱性は override / バージョン更新で直すこと。ここには載せない。
#
# --- distroless(gcr.io/distroless/nodejs24-debian12) ベースの libssl3(OpenSSL) OS CVE ---
# distroless は apt を持たず自前でパッチできない。上流がベースを再ビルドすれば解消するため、
# イメージを定期的に再ビルド（ベース再取得）して解消を確認し、解消済みのものは本ファイルから削除する。
# 登録日: 2026-07-03 / 次回見直し目安: 2026-08 のベース再取得時
CVE-2026-31789
CVE-2026-28387
CVE-2026-28388
CVE-2026-28389
CVE-2026-28390
CVE-2026-45447
